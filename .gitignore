# scratch / tooling dirs (not part of the project)
.tmp/
.agents/

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# editors
.vscode/
.idea/
*.swp

# npm is only ever used by build-dist.ps1, and only for the dist-min bundle
node_modules/

# deploy variants — generated by build-dist.ps1, never edited by hand.
# The repo root is itself servable, so Pages does not need these.
dist/
dist-cdn/
dist-min/
